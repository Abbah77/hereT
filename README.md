Rules: delete or replace anything that is not used for the refactor.

first story should be the user story fixed hard-coded with +btn,clicking the +btn should should open another full page i will handle upload story there dont worry about just make the routing i will create the page,and clicking the story body not btn should open user story for him to view like how he use to view his friends own thesame widget avoid coding 2 UI
story section should be in realtime if user is interacting with the page then his friend upload a story no need for the user to refresh,its not bad for the user to refresh but it should not wait for the user.
the page pull-refresh if user pull and refresh it make the screen blank and loading in the middle with is wrong,it should only refresh the contents and while refrshing it should make the screen blur as good UX so if it refresh it should display normal,the blur is a good feedback to the user.
if user change page on bottom nav and return back to the home the story refresh which is bad ux it should stop jist realtime data.
instead of displaying icon for the top left bar it should display the app logo(images/logo.png).
the post should be Facebook-style feed uses a single reusable card per post. The card has fixed sections like header (user info), body (content), and footer (actions). The body is dynamic: it renders text, single/multiple images, or video depending on the post type. This keeps the UI consistent while supporting different content types efficiently and infinit scrolling like facebook if it reach some long it should continue loading.
Touching the home btn while still on the page should refresh the page content so even if user scroll many post he can refresh to top quickly like tiktok with good UI/UX
search bar at the middle top in between the app name and notification, it should be highly premium and luxury so user can search any content and realtime serch user typing and it should be displaying he can click search to open a new page search, i will handle the page creation later just make the routing, and the space that appear within the page The search suggestions dropdown is a floating container below the search input that dynamically shows matching results or recent searches as the user types he can decide use the shortcut or click the search to move to the search page.
